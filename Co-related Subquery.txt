SELECT * FROM EMPWHERE DEPTNO IN(SELECT DEPTNO FROM EMPWHERE EMP.DEPTNO=DEPT.DEPTNO);Here, the employee table not presented in the inner query so , the outer query will give the input details then it checks the all the matched valuesSUB-QUERY OPERATOR:1. EXIST OPERATOR        It is a unary operator which can accepts only one operand towards RHS. And the operand must be a co-related subquery.	EXIST operator returns TRUE if sub-query returns a value.ex:Query to display in which emp's are workingSQL> SELECT * FROM DEPT	WHERE EXISTS (SELECT DEPTNO FROM EMP	WHERE EMP.DEPTNO=DEPT.DEPTNO);2. NOT EXIST OPERATOR	It is a unary operator which can accepts only one operand towards RHS. And the operand must be a co-related subquery.	NOT EXISTS operator return TRUE if subquery does not returns any value or it returns NULL value.ex:Query to display dept details in which no employees are workingSQL> SELECT * FROM DEPT	WHERE NOT EXISTS( SELECT DEPTNO FROM EMP	WHERE EMP.DEPTNO=DEPT.DEPTNO);NTH MAXIMUM AND NTH MINIMUM VALUESMAX:	SELECT SAL	FROM EMP E1	WHERE N-1 IN(SELCT COUNT(DISTINCT SAL)			FROM EMP E2			WHERE E1.SAL<E2.SAL);MIN:	SELECT SAL	FROM EMP E1	WHERE N-1 IN(SELECT COUNT(DISTINCT SAL)			FROM EMP E2			WHERE E1.SAL>E2.SAL);1. Query to display the 12th maximum salary.SQL> SELECT25-03-26-----------------------------------------------------Difference between subquery and co-related subquery1. In sub-query the execution starts with inner query1. In co-related sub-query the execution starts with outer query2. In sub-query only the outer query depends on the inner query2. In co-related subquery both inner query and outer query interdependent.3. In sub-query we should not use any concept of joins3. In co-related query we have to use the concept of joins4.In sub-query the outer query executes only one time.4. In co-related sub query the outer query executes twice.PSEUDO COLUMNSThese are the hidden or false columns present in each and every tables by default.We have two types of pseudo columns 	1. Row id	2. Row numRow id:	It is a 18 bit unique address of each and every record present in the table.	It can be used to identify other records uniquely from the table.	Row id is always a static value.	Row id's are autogenerated at the time of insertion of the record into the table.EX:	SQL>SELECT EMP.*,ROWID FROM EMP;Row num:	It is a serial number assigned to the result table.	Row num always starts with 1.	Row num is the dynamic value.	Row num generates at the time of execution of the queries.Query to display 4th record from the emp tableSQL> SELECT * FROM	(SELECT EMP.*,ROWNUM RN FROM EMP)	WHERE RN=4Query to display the first 5 records from emp table tableSQL>  SELECT * from (SELECT EMP.*,ROWNUM RN FROM EMP)	WHERE ROWNUM BETWEEN 1 AND 5;Query to display the last five record from emp tableSQL> SELECT * FROM (SELECT EMP.*,ROWNUM RN FROM EMP)         WHERE RN>(SELECT COUNT(*) FROM EMP)-5;SQL>  SELECT * FROM (SELECT EMP.*,ROWNUM RN FROM EMP)	WHERE RN>(SELECT COUNT(*)-5 FROM EMP);TYPES OF SINGLE ROW FUNCTIONS1. LENGTH():	It is used to identify the number of characters present in the given value or strSing.	SYNTAX::		LENGTH('STRING')	Whenever we take any user given values that must be present with in the single quotes ('')	but for thee column name we should not give the single quotes.Query to display no.of characters present in the given value 'Qspiders'SQL>SELECT LENGTH('Qspiders') FROM DUAL;Note:------DUAL TABLE:	It is the dummy table present in each and every database by defaulty which is consist of an empty row and an empty column	And it can be used to take the user given inputs and generates the outputs.Quert to display name of the employees along with no.of characters present in their namesSQL>SELECT ENAME,LENGTH(ENAME) FROM EMP;Query to display details of the employee who has exactly 5 characters in their namesSQL> SELECT * FROM EMP	WHERE LENGTH(ENAME)=5;Query to display details of the employees who all are earning 4 digits salary SELECT * FROM EMPWHERE LENGTH(SAL)=4;28-03-26-----------------------SUBSTR()	It is used to fetch part of a string from the given original string.	SYNTAX:		SUBSTR('ORIGINAL_STRING',STARTING_POS,END_POS)EX: SELECT SUBSTR('PUNJAGUTTA',1,3) FROM DUAL;SUB---PUNQuery to display last three character of the string PUNJAGUTTASELECT SUBSTR('PUNJAGUTTA',-3) FROM DUALQuery to display first three characters of each ename.SELECT SUBSTR(ENAME,1,3) FROM EMP;Query to display last five characters of each ename.SELECT SUBSTR(ENAME,-5) FROM EMP;Query to display details of the employees whose names are starting with Vowels.SELECT * FROM EMPWHERE SUBSTR(ENAME,1,1) IN ('A','E','I','O','U');Query to display details of the employees whose names are not ending with vowelsSELECT * FROM EMPWHERE SUBSTR(ENAME,-1) NOT IN('A','E','I','O','U');INSTR():-----------------------	It is used to fetch position of a string inside the given original string	Syntax:		INSTR('ORIGINAL_STR',POSITION[,OCCURANCE])EX:SELECT INSTR('MALAYALAM','A',1) FROM DUAL;Query to display names of the employees along with position of character A present in their namesSELECT ENAME,INSTR(ENAME,'A',1) FROM EMP;Query to display details of the employees whose name contains character ASELECT * FROM EMPWHERE INSTR(ENAME,'A',1)>0;Query to display details of the employees whose job contains two times or more A.SELECT * FROM EMPWHERE INSTR(JOB,'A',1,2)>1;ABS():-----------   It id used to convert negative values into positive values.   SYNTAX:	ABS(VALUE)EX:SELECT ABS(-4567) FROM DUAL;	ROUND():--------------	It is used to round of the values based upon the scale values.SYNTAX:ROUND(VALUE,[,SCALE])EX:SELECT ROUND(5678.4673456823579,-3) FROM DUAL;(5678.4673456823579,-3)-----------------------                   6000SELECT ROUND(5678.4567) FROM DUAL;(5678.4567)-----------       5678TRUNC():-------------------	It is similar to the round function but it always round of the values to the lower values.SYNTAX:TRUNC(VALUE,['SCALE])EX:SELECT TRUNC(5678.47969389896865993659829759,-2) FROM DUAL;30-03-2026-------------------MONTHS_BETWEEN():	It is used to calculate the no of months present between given two dates.SYNTAX:	MONTHS_BETWEEN('DATE1','DATE2')Query to display names of the employees along with their experience in months.SELECT ENAME,MONTHS_BETWEEN(SYSDATE,HIREDATE) FROM EMP;Query to display details of the employee along with his experience in years.SELECT EMP.*,MONTHS_BETWEEN(SYSDATE,HIREDATE)/12FROM EMP;Query to display details of the employees who are having experience of 45 years or more than that.	SELECT * FROM EMPWHERE MONTHS_BETWEEN(SYSDATE,HIREDATE)/12 >=45;ADD_MONTHS()	It is used to add OR subtract the months inside the given dateSYNTAX:	ADD_MONTHS('DATE',NO.OF MONTHS)NEXT_DAY()	It is used to give next date of the given day from the given date.Ex:SELECT NEXT_DAY(SYSDATE,'THURSDAY')FROM DUAL;LAST_DAY()	It is used to fetch last date of the given month from the given dateSYNTAX:	LAST_DAY('DATE')SELECT LAST_DAY(SYSDATE)FROM DUAL;TO_CHAR()	It is used to convert in to the string format based up on the format modelsformat models:	year = TWENTY TWENTY-SIX	YYYY = 2026	YY   = 26	MONTH = MARCH	MON   = MAR	MM    = 03	DAY   = MONDAY	DY   = MON	D    = 02	DD   = 30	HH24 = 19:00	HH12 =  7:00	MI   =  02 MINUTES	SS  = 20 SECONDS	HH24:MI:SS = 19:02:20	HH12:MI:SS = 07:02:20	DD-MON-YY  = 30-MAR-2026	DD-MON-YYYY = 30-MAR-2026SELECT TO_CHAR(SYSDATE,'HH24')FROM DUAL;SELECT TO_CHAR(SYSDATE,'HH12:MI:SS')FROM DUAL; SELECT TO_CHAR(SYSDATE,'YEAR') FROM DUAL;NVL2():	It is similar to NVL() but it consists of 3 arguments.If argument 1 contains some values then it will display the second argument & if argument consists of NVL valuesand it will display the 3rd argumentQuery to display name, salary, comm along with salary + bonus if employees are earning comm then add the bonus as 5000if employees are not earning the comm then add the bonus as 10000.SELECT ENAME,SAL,COMM,COMM+NVL2(COMM,5000,10000)FROM EMP;31-03-2026---------------------------------------------------It is used to assign the unique values to each and every record based upon the given value.If value is same it will assign the same rank to both the records. But for the next record it will skips the rank valueSYNTAX:	RANK() OVER([PARTITION BY_COLNAME]  ORDER BY COL_NAME[DESC])Query to display name salary of the employee along with rank based up on the their salary in ascending order.SELECT ENAME,SAL,RANK() OVER (ORDER BY SAL)FROM EMP;Query to display details of the eployees along with ranks based up on their salaries in descending orderfor each departmentSELECT EMP.*,RANK() OVER (PARTITION BY DEPTNO ORDER BY SAL DESC)FROM EMPDENSE_RANK():-------------	It is similar to the rank function but it will not skip the rank values.SYNTAX:DENSE_RANK() OVER ([PARTITION BY COL_NAME] ORDER BY COL_NAME)Query to display name and commission of the employee also mention one command  ,if employees earning commission then commin if the employee not earning comm not availableSELECT ENAME,COMM,NVL2(COMM,'AVAILABLE','NOT AVAILABLE')FROM EMPQuery to display names and comm of the employee if comm is there then display the commission if comm is notthere then display not available.SELECT ENAME,COMM,NVL(TO_CHAR(COMM),'NOT AVAILABLE')FROM EMPCASE():-----------------	It is a kind of conditional statement which is used to display the custom outputs.SYNTAX:	CASE	  WHEN <STATEMENT> THEN <OUTPUT>	  WHEN <STATEMENT> THEN <OUTPUT>	  ENDQuery to display name, salary along with the comment if employee is earning salary more than 2000 and mention the comment as earningmore than 2000 ,if he is earning between 1500 to 2000 mention the comment as earning between 1500 to 2000if he is earning lesser than 1500 then mention the comment earning less than 1500	SELECT ENAME,SAL,CASE                 WHEN SAL > 2000 THEN 'EARNING MORETHAN 2000'                 WHEN SAL BETWEEN 1500 AND 2000 THEN 'EARNING BETWEEN 1500 TO 2000'                 WHEN SAL <1500 THEN 'EARNING LESSER THAN 1500'                 END CMNTFROM EMP;SET OPERATORS:--------------------------------------------------------------------------	It is used to write multiple queries in single executionTYPES:1. UNION2. UNION ALL3. INTERSECT4. MINUS1.UNION :	It is used to fetch the record from the both the queries without repetition. SELECT * FROM EMP WHERE DEPTNO IN(10,20) UNION SELECT * FROM EMP WHERE DEPTNO IN(20,30);2. UNION ALL:	It is used to fetch the record from the both the queries with the repetitionSELECT * FROM EMPWHERE DEPTNO IN (10,20)UNION ALLSELECT * FROM EMPWHERE DEPTNO IN(20,30)3. INTERSECT:	It is used to fetch the records from the both queries when only records are matched OR common.SELECT * FROM EMPWHERE DEPTNO IN(10,20)INTERSECTSELECT * FROM EMPWHERE DEPTNO IN(20,30);4.MINUS:	It is used to fetch the records from the both queries only un common records from the first query. SELECT * FROM EMP WHERE DEPTNO IN(10,20) MINUS SELECT * FROM EMP WHERE DEPTNO IN(20,30)SELECT * FROM EMPWHERE TO_CHAR(HIREDATE,'DY')='SUN';01-04-2026DDL(DATA DEFINITION LANGUAGE)---------------------------------------------------------------------------------	It is used to create or modify the structure of a data base object.	There are total five statements comes under DDL.	Those are,	1. CREATE	2. ALTER	3. TRUNCATE	4. RENAME	5. DROPHow to create a database================================CONN user-name: SYSTEM password: tigerCREATE USER MAHESH IDENTIFIED BY MAHESH;GRANT DBA TO MAHESHCONNCONN user-name: MAHESH password: MAHESHSHOW USER;	  1. CREATE::	It is used to create the table inside the database.		SYNTAX:	 CREATE TABLE TABLE_NAME	 ( COL_NAME1 DATATYPE CONSTRAINTS,	 ( COL_NAME2 DATATYPE CONSTRAINTS.		||		||	(COL_NAMEN DATATYPE CONSTRAINTS);Creating a new table ,studentsCREATE TABLE STUDENTS( SID NUMBER(6) PRIMARY KEY,SNAME VARCHAR(20) NOT NULL,PHONE NUMBER(10) UNIQUE NOT NULL CHECK(LENGTH(PHONE)=10),GENDER VARCHAR(6),DOB DATE);RENAME:==============================	It is used to change the name of the database object.	SYNTAX:	 RENAME OLD_TABLENAME TO NEW_TABLENAME;EX:    RENAME STUDENTS TO STUDENT_DETAILS;ALTER:===============================	It is used to modify the structure of a database object.   1. TO ADD A COLUMN:	SYNTAX:	 ALTER TABLE TABLE_NAME	 ADD COLUMN_NAME DATATYPE CONSTRAINTS;		EX:	   ALTER TABLE STUDENT           ADD EMAIL VARCHAR(50) UNIQUE;    2. TO RENAME A COLUMN:	SYNTAX:	  ALTER TABLE TABLE_NAME	  RENAME COLUMN OLD_COLNAME TO NEW_COLNAME;  	EX: 	  ALTER TABLE STUDENT          RENAME COLUMN PHONE TO MNO;    3. TO MODIFY DATATYPE:	SYNTAX:	  ALTER TABLE TABLE_NAME	  MODIFY COLUMN_NAME NEW_DATATYPE;    	EX:	  ALTER TABLE STUDENT          MODIFY SID VARCHAR(20);     4. TO MODIFY THE CONSTRAINT:	  SYNTAX:	    ALTER TABLE TABLE_NAME	    MODIFY COL_NAME OLD_DATATYPE NEW_CONSTRAINT;          EX:	    ALTER TABLE STUDENT            MODIFY DOB DATE NOT NULL;     5. TO DROP A CONSTRAINT:	 SYNTAX:	   ALTER TABLE TABLE_NAME	   DROP CONSTRAINT CONSTRAINT_REF_NAME;	 EX:           ALTER TABLE STUDENT           DROP CONSTRAINT SYS_C005433;     6. TO ADD A CONSTRAINT REFERENCE NAME:         SYNTAX:	   ALTER TABLE TABLE_NAME	   ADD CONSTRAINT CONST_REF_NAME CONST_TYPE(COL_NAME);	 EX:           ALTER TABLE STUDENT           ADD CONSTRAINT UN_MNO UNIQUE(MNO);  //MAHESH      UN_MNO      U STUDENT     7. TO DROP A COLUMN:	SYNTAX:	   ALTER TABLE TABLE_NAME	   DROP COLUMN COLUMN_NAME;	EX:           ALTER TABLE STUDENT           DROP COLUMN EMAIL;      8. TO ASSIGN A FOREIGN KEY:	  SYNTAX:            ALTER TABLE TABLE_NAME	    ADD CONSTRAINT CONST_REF_NAME FOREIGN KEY(COL_NAME)	    REFERENCES PARENT_TABLE_NAME(COL_NAME);           EX:                            CREATE TABLE TEACHERS               (TID NUMBER(6) PRIMARY KEY,               TNAME VARCHAR(20) NOT NULL,               SUBJECT VARCHAR(10) );      9. 		It is used to remove all the records from the table permanently.	SYNTAX:	  TRUNCATE TABLE TABLE_NAME        EX:   COPYING A TABLE:    CREATE TABLE TABLE_NAME    AS    SELECT * FROM TABLE_NAME;     EX:	TRUNCATE TABLE EMP3;    DROP:    ===============================       It is used to remove the table from the database.      SYNTAX:         DROP TABLE TABLE_NAME;      EX: 	DROP TABLE EMP3;		FLASHBACK: 		============================		FLASHBACK TABLE TABLE_NAME                TO BEFORE DROP;			FLASHBACK TABLE EMP3                TO BEFORE DROP;    PURGE:============================  It is used to remove the table from the recycle bin.SYNTAX:::: PURGE TABLE TABLE_NAME;EX:      PURGE TABLE EMP3;DATA MANIPULATION LANGUAGE:---------------------------------------------------------------------------------------------------------------------------	It is used to manipulate the data inside the table.	There are three sub statements comes under DML	1. Insert	2. Delete	3. UpdateINSERT:=========================================	It is used to insert or add the record inside the table.	SYNTAX:	INSERT INTO TABLE_NAME(COL_NAME1,COL_NAME2,.....COL_NAMEN)	VALUES(VALUE1,VALUE2,......VALUEN);		EX:03-04-26-----------------------------------------------------------------------------------------------------------------------------------------------------------UPDATE:=====================	It is used to update the data inside the table.	SYNTAX:	UPDATE TABLE_NAME	SET COL_NAME = VALUE,COL_NAME=VALUE	[WHERE <FILTER CONDITION>];		EX:	UPDATE EMP        SET ENAME='MAHESH',SAL=8000        WHERE ENAME='SMITH';DELETE:=====================	It is used to delete the records from the table	SYNTAX:	DELETE FROM TABLE_NAME	WHERE <FILTER CONDITION>;	EX:	DELETE FROM EMP	WHERE SAL=8000;TRANSACTION CONTROL LANGUAGE:----------------------------------------------------------------	It is used to control the transactions performed on the database.	There are three statements comes under TCL.	1. COMMIT	2. ROLLBACK	3. SAVEPOINT1. COMMIT:==========================================	It is used to save the transactions performed on the database.	SYNTAX: COMMIT;	2.ROLLBACK:============================================	It is used to take us back to the last saved location.	SYNTAX: ROLL BACK;3.SAVE POINT:============================================	It is used to mark the position.	SYNTAX: 	SAVEPOINT;DCL:===========	It is used to control the data flow between the users.	There are two types.	1.GRANT	2.REVOKE1.	It is used to grant the permission to accept or work on the data.	SYNTAX:	GRANT ALL	ON TABLE_NAME	TO USER_NAME;	EX:	GRANT ALL        ON EMP        TO HR;	SELECT * FROM SCOTT.EMP;2.REVOKE:=============================================	It is used to take back the permission or revoke back the data from the user.	SYNTAX:	REVOKE ALL        ON TABLE_NAME        TO USER_NAME;	EX:	REVOKE ALL        ON EMP        FROM HR;NORMALIZATION:-------------------------------------------------------------------------------	It is the process of converting a larger a table into smaller tables in order to remove the redundancies and anamoliespresent in the table by identifying their functional dependencies is called as NORMALIZATION.	OR		It is the process of decomposing a larger table into the smaller tables 	OR		The process of converting a table to its normal form it known as NORMALIZATION.REDUNDANCY:======================================Repetition of unwanted of data is known as REDUNDANCY.ANAMOLY:=====================================Side effect occurs due to DML operations is known as ANAMOLY.There are like insertion, deletion, updationFUNCTIONAL DEPENDENCY:---------------------------There existing are dependency such that are attribute is determined by another attribute uniquely.It is known as FUNCTIONAL DEPENDENCY.TYPES OF FUNCTIONAL DEPENDENCIES======================================================There are three types of functional dependency present.1. Total functional dependency.2. Partial functional dependency.3. Transitive functional dependency.ATTRIBUTE:-------------------------------Types of attributes1. KEY ATTRIBUTE (CANDIDATE KEY)2. NON-KEY ATTRIBUTE 3. PRIME KEY ATTRIBUTE4. NON-PRIME KEY ATTRIBUTE (ALTERNATIVE KEY)5. FOREIGN KEY ATTRIBUTE6. COMPOSITE KEY ATTRIBUTE7. SUPER KEY ATTRIBUTE1.KEY ATTRIBUTE:======================The attributes which is able to identify records uniquely is known as KEY ATTRIBUTE.2.NON-ATTRIBUTE:=================Apart from key attributes all the other attributes are known as NON-KEY ATTRIBUTE3.PRIME-KEY ATTRIBUTE:=======================Among all the key attributes we will select one attribute to become a primary key of the table is known as PRIME-KEY ATTRIBUTE.4.NON-PRIME KEY ATTRIBUTE:===============================Apart from primary key all the other key attributes are known as non prime key attributes.5. FOREIGN KEY ATTRIBUTE:============================Attribute which is used to establish the connection between two tables is known as foreign key attribute.6. COMPOSITE KEY ATTRIBUTE:===============================In the absence of a key attribute to identify the records uniquely we will take the combination of two or more non key attributes is known as composite key attribute.                                                                                                                                                                                                                                                                                                                                           7. SUPER KEY ATTRIBUTE:=========================Combination of all the key attributes along with prime key attributes is known as super key attribute.PARTIAL FUNCTIONAL DEPENDENCY:---------------------------------------TRANSITIVE FUNCTIONAL DEPENDENCY:-------------------------------------There exists are dependency such that in the presence of a key attribute a non attribute is determining another non key attribute uniquelyit is known as transitive functional dependency. But internally both determined by the key attribute only.FORMS OF NORMALIZATION:===========================1NF2NF3NF3.5NF/BOYCE CODD'S 1NF:----To achieve first normal form table should have to follow two rules:1. duplicated record should not be present.2. Multi valued data should not be present.2NF:-----To achieve second normal form table should have to follow two rules:1. Table must be in first normal form2. Partial functional dependency should not be present.UPDATE EMPSET ADEPTNO=SUBSTR(DEPTNO,INSTR(DEPTNO,',',1,1)+1)WHERE INSTR(DEPTNO,',',1,1)>0;UPDATE EMPSET ADEPTNO=SUBSTR(DEPTNO,1,INSTR(DEPTNO,',',1,1)-1)WHERE INSTR(DEPTNO,',',1,1,)>0;3NF:c-----To achieve third normal form table should have to follow two rules:1.Table must be in second normal form2. Transitive functional dependency should not be present.3.5NF:------It is a updated version of third normal form. It states that dependency should alwayslies from right to leftTo achieve 3.5 NF Complex view:---------------------------The view which we are create by making columns from multiple tableswith the help of join or any foreign primary key conditionIn complex view the columns which we are taking from the child table we can update that columns easily but the columns which we are taking from the parent table we can't update that columns.GRANT CREATE VIEW TO SCOTT;SYNTAX:CREATE VIEW VIEW_NAMEASSELECT <STMT USING JOINS OR CONSTRAINS>;EX: CREATE VIEW V2 AS SELECT ENAME,JOB,DNAME  FROM EMP,DEPT WHERE EMP.DEPTNO=DEPT.DEPTNO;MATERIALIZED VIEW:GRANT CREATE MATERIALIZED VIEW TO SCOTT;It is used to store the result set of SQL query.We can't update anything inside the materialized view.SYNTAX:CREATE MATERIALIZED VIEW VIEW_NAMEASSELECT <STMT>;SEQUENCE:These are the auto generated serial numbers by the database.Sequence are of two types:	1. Default sequence	2.Customized sequencesDefault sequence: It always starts from 1 and increments by 1.SYNTAX:CREATE SEQUENCE SEQ_NAME; CREATE SEQUENCE S1;CUSTOMIZED SEQUENCE: The sequence which user will customized according to the requirements that is known as customized sequence. SYNTAX:  CREATE SEQUENCE SEQ_NAME  START WITH VALUE  INCREMENT BY VALUE;CREATE SEQUENCE S2START WITH 1001INCREMENT BY 7;INSERT INTO TT VALUES(S1.NEXTVAL,S2.NEXTVAL,'&C3');SELECT * FROM TT;   C1         C2 C3----- ---------- --------------------    1       1001 DIVYA    2       1008 MAHESH    3       1015 BOBBY    4       1022 RAMYASYNONYMS:=====================These are the alternatively given to the database objectsSYNTAX:CREATE [PUBLIC] SYNONYM SYNONYM_NAMEFOR OBJECT_NAME;GRANT CREATE PUBLIC SYNONYM TO SCOTT;GRANT CREATE SYNONYM TO SCOTT;Synonyms are two types:1.Private synonym2.Public synonymCREATE SYNONYM SG FOR SALGRADE;CREATE PUBLIC SYNONYM SGG FOR SALGRADE; SELECT * FROM SCOTT.SALGRADE; SELECT * FROM SCOTT.SG; //PRIVATE SELECT * FROM SGG; //PUBLIC			          	     	 		